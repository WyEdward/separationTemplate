server:
  port: 8084
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/commontemplate?useUnicode=true&characterEncoding=utf-8&useSSL=false&&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

#mybatis配置
mybatis-plus:
  typeAliasesPackage: cn.wyedward.core.entity
  mapper-locations: classpath:mappers/**/*Mapper.xml
  global-config:
    db-config:
      table-prefix: sr_
